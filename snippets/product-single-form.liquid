{% capture "form_classes" -%}
  product-form product-form-{{ section.id }}{% if section.settings.enable_payment_button and product.has_only_default_variant%} product-form--payment-button-no-variants{% endif %}{% if inline_button %} flex{% endif %}
{%- endcapture %}

{%- render 'product-single-form-inner' with current_variant: current_variant, full_qty_box: full_qty_box, inline_button: inline_button, align_center_button: align_center_button, product: product, current_variant: current_variant, form_classes: form_classes, left_holder: left_holder -%}